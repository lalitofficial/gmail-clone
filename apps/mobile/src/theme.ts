import { tokens } from '@gmail-clone/shared';

export { tokens };
export const c = tokens.color;

/** React Native needs the exact loaded font family per weight. */
export const fontFamily = {
  regular: 'Roboto_400Regular',
  medium: 'Roboto_500Medium',
  bold: 'Roboto_700Bold',
};
